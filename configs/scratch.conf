# configs/scratch.conf — 手动单跑迭代模板（对齐 SurvPGC configs/scratch.conf）
#
# 用法：改下面三行，然后 bash configs/run.sh configs/scratch.conf
# （也可以直接 python configs/tools/runner.py task configs/scratch.conf --dry-run 先看命令）
#
# 需要改哪个模型就填哪个实验+模型：模型规格全部来自 registry
# （configs/z_gen/common.py 的 MODELS 表 + defaults.conf），
# 这里只覆盖任务选择。

EXPERIMENT_ID=singlemodal_eval
TASK=model_run
MODEL_ID=V3_3B
FORCE=false
